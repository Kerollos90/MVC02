@using Company.Service.Interface.EmployeeInterface.Dto
@model IEnumerable<EmployeeDto>
@{
    ViewData["Title"] = "View";
}
<a asp-controller="Employee" asp-action="Create" class="btn btn-primary ">Add New Employee</a>


@* <div class="mt-5 alert alert-danger">
    <h3>@ViewBag.message</h3>
</div>
<div class="mt-5 alert alert-danger">
    <h3>@ViewData["test message"]</h3>
</div>
<div class="mt-5 alert alert-danger">
    <h3>@TempData["test temp message"]</h3>
</div> *@

<div class="row justify-content-center align-items-center mt-5">
    <form class="row col-8 offset-2" asp-action="Index">

        <div class="col-8">

            <input type="text" class="form-control" name="searchInp" palceholder="Search By Name" />
        </div>




        <div class="col-4">

            <input type="submit" value="Search" class="btn btn-success" />
        </div>


    </form>
</div>

@if (Model.Count() > 0)
{
    <table class="table table-striped table table-hover">

        <thead>
            <tr>
                <th>@Html.DisplayNameFor(D => D.Id)</th>
                <th>@Html.DisplayNameFor(D => D.Name)</th>
                <th>@Html.DisplayNameFor(D => D.CreateAt)</th>
                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>


        </thead>

        <tbody>
            @foreach (var employee in Model)
            {
                <tr>

                    <td>@employee.Id</td>
                    <td>@employee.Name</td>
                    <td>@employee.CreateAt</td>


                    <partial name="_ActionButtonPartialView" model="@employee.Id.ToString()" />

                </tr>





            }







        </tbody>













    </table>


}
else
{
    <div class="mt-5 alert-warning">
        <h3> There is no Employee</h3>
    </div>


}
